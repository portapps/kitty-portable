version: ~> 1.0

os: windows
language: shell

before_install:
  - export PORTAPPS_VERSION=$(cat go.mod | grep github.com/portapps/portapps | awk '{print $NF}')

import:
  - portapps/portapps:.travis/configs/install.yml@master
  - portapps/portapps:.travis/configs/build.yml@master
  - portapps/portapps:.travis/configs/set-version.yml@master
  - portapps/portapps:.travis/configs/git-config.yml@master
  - portapps/portapps:.travis/configs/deploy.yml@master
  - portapps/portapps:.travis/configs/notifs.yml@master
